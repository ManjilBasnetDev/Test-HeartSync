# Firebase Configuration
firebase.auth.token=YOUR_FIREBASE_AUTH_TOKEN

# Replace YOUR_FIREBASE_AUTH_TOKEN with the actual token from:
# 1. Firebase Console > Project Settings > Service Accounts
# 2. Generate New Private Key
# 3. Use the private key to generate an auth token 